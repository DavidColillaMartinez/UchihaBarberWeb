# SES-021 — Corrección de coherencia y controles de cierre

Agente: Codex en ChatGPT. Base publicada revisada: `5b45e4ca21b55ba7ee9038d2f6da12f1f81281d2`. Alcance autorizado: actualización documental y comportamiento de las IAs tras la auditoría. Entrega como parche para aplicar mediante OpenCode; commit/push local del usuario todavía no realizados por esta entrega.

## Contraste realizado

Se compararon PRODUCT, AGENTS, mapa, estado, specs y relevos con los archivos publicados. Servicios estaba aceptado (SES-013 y SES-020), pero faltaba su spec y la excepción de snap general; se consolidan sin alterar su implementación. Galería ya era carrusel aceptado, no banner pendiente; su transición de fondo no queda listada como trabajo por hacer. Las familias Inter/Archivo y vídeos locales se identifican como demos autorizadas, conservando la elección final pendiente.

Se actualizaron las fuentes vigentes y la evidencia de Impeccable (ejecuciones mecánicas reportadas en los relevos, no una validación visual universal). No se reescriben los relevos históricos ni se inventa un ciclo OpenSpec de Servicios.

## Prevención

Registro por bloque con rutas, specs, aceptación y pendientes. Cierre con revisión cruzada obligatoria incluso en una misma conversación o tras compactar. Check de cobertura, duplicados, referencias y huella de revisión; escanear no modifica esa revisión. Comando de nuevos relevos con asignación exclusiva y legado de las tres colisiones anteriores. Ningún comando declara aprobación del usuario por sí solo.

## Verificación y límites

Se ejecutaron las 14 pruebas (6 del inventario y 8 de coherencia), todas correctas, sin omitidas. Incluyen asignación concurrente de relevos y detección de cambios posteriores a la revisión. El inventario conserva los hashes publicados de recursos binarios no modificados. OpenCode debe aplicar el parche sobre una copia compatible, validar las siete specs con su CLI, ejecutar las pruebas y el check completos con los recursos locales y registrar los resultados reales en un nuevo relevo. No se ejecuta build ni revisión visual porque no se modifica implementación web.

## Siguiente paso

Integrar el parche sin descartar cambios posteriores, validar la consolidación y actualizar este pendiente mediante el siguiente relevo. No pedir al usuario que lea AGENTS o administre IDs. Tras integrar, continuar exclusivamente con el bloque que el usuario autorice. SHA propio de esta entrega: pendiente de la aplicación y commit de OpenCode; no se inventa ni se persigue con commits autorreferentes.
