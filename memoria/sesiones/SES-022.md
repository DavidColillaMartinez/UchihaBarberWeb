# SES-022 — Aplicación del parche de coherencia

Agente: OpenCode (modelo kimi-k3). Fase: UB-004 (mantenimiento de coherencia documental y controles de cierre). Modalidad: aplicación del parche `UchihaBarber_actualizacion_coherencia.patch` facilitado por Codex, según SES-021 y DEC-002. Commit/push autorizados por el responsable en su instrucción.

## Alcance y procedencia

Base Git real: `5b45e4ca21b55ba7ee9038d2f6da12f1f81281d2` (`main`), remoto `UchihaBarberWeb` sincronizado (0 divergencia en `git fetch`). Árbol limpio antes de aplicar: el parche pasó `git apply --check` integral y no hubo trabajo posterior ni ajeno que reconciliar.

Continuidad revisada antes de comenzar: `AGENTS.md`, `PRODUCT.md`, `memoria/README.md`, `memoria/protocolo.md`, `memoria/estado.json`, `memoria/integraciones.md` y el relevo `SES-020` como `ultimoRelevo`. `memoria:check` previo: vigente. El contenido del parche fue leído completo antes de aplicarlo.

Distribución del parche (leída en su totalidad): añade los controles de coherencia (`bloques.json`, `cierre.md`, `revision.json`, `relevo.mjs`, `coherencia.mjs` + pruebas), consolida la spec `home-servicios` desde sus relevos y código, refleja en `home-skeleton` la excepción de snap de Servicios, y actualiza la documentación canónica (AGENTS, PRODUCT, memoria, config). Como en SES-021, solo modifica la web un comentario documental en `Servicios.astro` (procedencia de las descripciones); no se toca el diseño ni el comportamiento aprobados.

## Decisiones vigentes y fuentes contrastadas

Contraste real realizado sobre las fuentes y archivos publicados:

- `home-servicios` queda contra la implementación: transformada `top 30%`→`top -20%` con `scrub`, título fijo medido en reposo, vídeo nativo en bucle pausado fuera/fuera movimiento reducido, filas con `BookingLink` compartido. Concorda con los relevos SES-008/009/010/013/020 confirmados por el responsable.
- `home-skeleton` recoge la excepción de snap de Servicios (`scroll-snap-align: none`, `overflow-anchor: none` en su CSS), como exige; sin alterar el snap de Galería ni el recorrido común.
- `site-navigation` describe comportamiento de inversión blanco/negro; la excepción técnica `mix-blend-mode`→tema por luminancia y el retardo ~0,3 s→0,1 s quedaron registrados en SES-007-nav-scroll tras la revisión del responsable. No se reescribe la spec consolidada: su texto sigue comportamiento y no desmiente la solución vigente; la desviación queda pendiente como en DEC-002.
- `PRODUCT.md` distingue decisiones vigentes de intenciones pendientes y registra la excepción temporal de recursos demo (Inter/Archivo y vídeos locales) sin autorizar importaciones extra.
- `bloques.json` vincula cada sección a rutas, specs, aceptación y pendientes; cinco aprobados demo, cuatro provisionales en su turno, sin duplicados de IDs según `relevos-legado.json`.
- `estado.json` actualiza `ultimoRelevo` a este documento y marca aplicación completada; `DEC-002` documenta por qué se añadió el control sin fabricar un ciclo OpenSpec de Servicios.

## Verificaciones (comandos y resultados reales en este equipo)

- `openspec validate --specs` con CLI 1.13.0: 7/7 correctas (`home-cabecera`, `home-galeria`, `home-intro`, `home-servicios`, `home-skeleton`, `products-page`, `site-navigation`). Avisos informativos de longitud, sin errores.
- `npm run memoria:test`: 14/14 (6 de inventario en `core.test.mjs` + 8 de coherencia en `coherencia.test.mjs`); incluye asignación exclusiva de relevos y detección de cambios tras una revisión.
- `memoria:changes` tras aplicar: las 10 altas y 15 modificaciones listadas; enlace legitimo al siguiente relevo antes de registrar revision.
- Relevo inicial creado con `npm run memoria:relevo` (SES-022, número sin colisión); completado y enlazado al estado antes de registrar la revisión.
- Revisión registrada con `npm run memoria:revision -- --relevo <ruta> --resumen "<contraste real>"` tras la actualización de estado.
- `npm run memoria:scan` regenera `generado/` con el conjunto final y `npm run memoria:check` combina inventario y coherencia tras la revisión declarada.
- `npm run build` solo confirma que el comentario único en código compila; no se modifica implementación visual ni se ejecuta `astro check` por separado.

## Aceptación y siguiente paso

No se requirió aprobación visual: esta entrega es documental/herramientas, sin cambios en diseño ni comportamiento de la web. Aceptación de los 5 bloques demo (cabecera, entrada, navegación, Servicios, Galería) sigue registrada en SES-020 y sus relevos anteriores.

Siguiente paso tras publicar: continuar exclusivamente con el bloque que el responsable autorice (acceso a Productos, Opiniones o Información dentro de UB-004; catálogo y móvil en sus fases). No anticipar secciones pendientes.
